[package]
name = "demo"
version = "1.2.3"

[dependencies]
plain = "1"
table = { version = "2", default-features = false, optional = true }
renamed = { package = "real-name", version = "0.3" }

[build-dependencies]
cc = "1.0"
